<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.home.authentic_art_works">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <permission
        android:name="com.example.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.permission.MAPS_RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginPage"
            android:label=""
            android:parentActivityName=".HomeScreen"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".SignupPage"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".SignupPage2"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".HomeMainActivity"
            android:label="@string/title_activity_home_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".IndividualActivity"
            android:label="@string/title_activity_individual"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MediaImagesActivity" />
        <activity android:name=".MediaVideosActivity" />
        <activity android:name=".FileActivity" />
        <activity android:name=".VideoActivity" />
        <activity android:name=".MediaFilesActivity" />
        <activity android:name=".UpdatesActivity" />
        <activity android:name=".NotificationsActivity" />
        <activity android:name=".PollsFullActivity" />
        <activity android:name=".FundRaiseFullActivity" />
        <activity android:name=".ImageFullActivity"></activity>
    </application>

</manifest>